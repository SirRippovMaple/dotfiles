# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $alt Mod1
set $special Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:MesloLGM NerdFont Mono 12

exec --no-startup-id discord --start-minimized
exec --no-startup-id picom -b
exec --no-startup-id udiskie --tray
exec --no-startup-id feh --no-fehbg --randomize --bg-fill ~/Pictures/wallpaper/*

bindsym $special+$alt+l exec loginctl lock-session

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use Mouse+$special to drag floating windows to their wanted position
floating_modifier $special

# start a terminal
bindsym $special+Return exec kitty

# Common applications
bindsym $special+b exec brave
bindsym $special+e exec kitty ranger
bindsym $special+n exec kitty newsboat
bindsym $special+c exec rofi -show calc -modi calc -no-show-match -no-sort
bindsym $special+v exec rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'

# VPN
bindsym $special+Shift+v exec expressvpn connect
bindsym $special+$alt+v exec expressvpn disconnect 

# kill focused window
bindsym $special+q kill

# Screenshots
bindsym Print "exec maim --hidecursor --select ~/Pictures/Screenshot-$(date -Iseconds | cut -d'+' -f1).png"
bindsym $alt+Sys_Req "exec maim --hidecursor --select | xclip -selection clipboard -t image/png"

# start dmenu (a program launcher)
#bindsym $special+d exec i3-dmenu-desktop
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $special+space exec rofi -show drun -modi drun -theme arthur -show combi -icon-theme "Papirus" -show-icons

# change focus
bindsym $special+h focus left
bindsym $special+j focus down
bindsym $special+k focus up
bindsym $special+l focus right

# alternatively, you can use the cursor keys:
bindsym $special+Left focus left
bindsym $special+Down focus down
bindsym $special+Up focus up
bindsym $special+Right focus right

# move focused window
bindsym $special+Shift+h move left
bindsym $special+Shift+j move down
bindsym $special+Shift+k move up
bindsym $special+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $special+Shift+Left move left
bindsym $special+Shift+Down move down
bindsym $special+Shift+Up move up
bindsym $special+Shift+Right move right

# split in horizontal orientation
bindsym $special+'-' split h

# split in vertical orientation
bindsym $special+'|' split v

# enter fullscreen mode for the focused container
bindsym $special+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $special+s layout stacking
bindsym $special+w layout tabbed
#bindsym $special+e layout toggle split

# toggle tiling / floating
bindsym $special+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $special+space focus mode_toggle

# focus the parent container
bindsym $special+a focus parent

# focus the child container
#bindsym $special+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $leftDisplay "DVI-I-1"
set $rightDisplay "DVI-D-0"

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

workspace $ws1 output $leftDisplay
workspace $ws2 output $leftDisplay
workspace $ws3 output $leftDisplay
workspace $ws4 output $leftDisplay
workspace $ws5 output $rightDisplay
workspace $ws6 output $rightDisplay
workspace $ws7 output $rightDisplay
workspace $ws8 output $rightDisplay
workspace $ws9 output $rightDisplay
workspace $ws10 output $rightDisplay

# switch to workspace
bindsym $special+1 workspace number $ws1
bindsym $special+2 workspace number $ws2
bindsym $special+3 workspace number $ws3
bindsym $special+4 workspace number $ws4
bindsym $special+5 workspace number $ws5
bindsym $special+6 workspace number $ws6
bindsym $special+7 workspace number $ws7
bindsym $special+8 workspace number $ws8
bindsym $special+9 workspace number $ws9
bindsym $special+0 workspace number $ws10

# move focused container to workspace
bindsym $special+Shift+1 move container to workspace number $ws1
bindsym $special+Shift+2 move container to workspace number $ws2
bindsym $special+Shift+3 move container to workspace number $ws3
bindsym $special+Shift+4 move container to workspace number $ws4
bindsym $special+Shift+5 move container to workspace number $ws5
bindsym $special+Shift+6 move container to workspace number $ws6
bindsym $special+Shift+7 move container to workspace number $ws7
bindsym $special+Shift+8 move container to workspace number $ws8
bindsym $special+Shift+9 move container to workspace number $ws9
bindsym $special+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $special+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $special+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $special+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $special+Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

# TODO List popup
for_window [instance="todo-list"] floating enable, resize set 1200 650;
for_window [instance="todo-list"] move scratchpad, scratchpad show, move position center, move scratchpad, scratchpad show
bindsym $special+t exec ~/.config/i3/todo; [instance="todo-list"] scratchpad show

# X-Air mixer
for_window [instance="X-AIR-Edit"] floating enable, move scratchpad, scratchpad show, move position center, move scratchpad, scratchpad show
bindsym $special+m exec ~/.config/i3/xairone; [instance="X-AIR-Edit"] scratchpad show

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym r resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $special+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $special+r mode "default"
}

bindsym $special+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#         status_command i3status
#         position top
# }
#
bar {
    i3bar_command ~/.config/polybar/launch.sh
}

default_border pixel
focus_follows_mouse no

assign [class="PrismLauncher" instance="prismlauncher"] $ws2
assign [class="Steam"] $ws2
assign [class="discord" instance="discord"] $ws4
assign [class="Skype" instance="skype"] $ws4
assign [class="Signal" instance="signal"] $ws4
assign [class="Minecraft" instance="Minecraft"] $ws6
assign [class="Paradox Launcher"] $ws6
assign [class="Cities.x64"] $ws6
assign [class="jetbrains-datagrip" instance="jetbrains-datagrip"] $ws8
assign [class="jetbrains-rider" instance="jetbrains-rider"] $ws10

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec light -A 5 # increase screen brightness
bindsym XF86MonBrightnessDown exec light -U 5 # decrease screen brightness

# ALSA controls
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%-
bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && refresh_i3status

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Display
bindsym XF86Display exec "lxrandr"

# Do not disturb
bindsym $special+Shift+n exec --no-startup-id ~/.config/dunst/dnd.sh -s
bindsym $special+Ctrl+n exec --no-startup-id ~/.config/dunst/dnd.sh -e

# Notifications
bindsym $special+shift+grave exec --no-startup-id dunstctl close
bindsym $special+ctrl+grave exec --no-startup-id dunstctl close-all
bindsym $special+grave exec --no-startup-id dunstctl history-pop

# color variables

set $bg-color           #20212B
set $inactive-bg-color  #20212B
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935

#window colors
#                       border              background         text                 indicator
client.focused          $bg-color           $bg-color          $text-color          #00ff00
client.unfocused        $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
client.focused_inactive $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          #00ff00

# disable window titlebars
for_window [class=".*"] border pixel 0
gaps inner 5
gaps outer 0

smart_gaps on
